- we started by a theoretical introduction to the field of IC design
- then we started in the cad tools video understand how does cad tools work to take our design and generate a bitstream of it
- then in the end to end FPGA project we took a general overview at how does the workflow in Vivado work  and we build a simple project which is a buffer where the output is always equal to the output using Verilog (buff.v)
- in structural representation we built two projects both of them were 2x1 multiplexers but one using gate flow modeling and the other using dataflow modeling the two projects are (mux_2x1_gate.v) and (mux_2x1_dataflow.v)
- in behavioral representation we created another 2x1 mux by using behavioral code that describes how a logic circuit works through a C similar code and the files we created are (mux_2x1_beh.v)
- then we started the hierarchical design chapter and in it we made a half adder then we used it to create a full adder and then we used it to create a four bit adder
- in generic code we made an n bit adder
- in subtractors we learned more about subtraction and adder_subtractor circuit and we even created a four bit and n bit adder subtractor
- then we learned more about arithmetic operations in Verilog and created an adder based on it
- after that we created a test bench for our adder subtractor in the developing testbench chapter